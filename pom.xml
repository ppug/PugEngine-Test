<project>
    <modelVersion>4.0.0</modelVersion>

    <groupId>xyz.australiasmp</groupId>
    <artifactId>pugengine-parent</artifactId>
    <version>dev-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>PugEngine (Parent)</name>

    <repositories>
        <repository>
            <id>aikar</id>
            <url>https://repo.aikar.co/content/groups/aikar/</url>
        </repository>
        <repository>
            <id>velocity-snapshots</id>
            <url>https://nexus.velocitypowered.com/repository/velocity-artifacts-snapshots/</url>
        </repository>
    </repositories>

    <build>
        <defaultGoal>install</defaultGoal>
        <finalName>${project.artifactId}</finalName>
    </build>
    <modules>
        <module>PugEngine-API</module>
        <module>PugEngine-Server</module>
        <module>Paper${file.separator}Paper-MojangAPI</module>
    </modules>
</project>
